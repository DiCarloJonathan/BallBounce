using UnityEngine;
using System.Collections;

public class guiStup : MonoBehaviour {

	// Use this for initialization
	void Start () {
		GUI.Button (new Rect(10,10,160,30), "where");

	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
